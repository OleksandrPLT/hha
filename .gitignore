# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# local SQLite database — never commit real guest/booking data. Anchored to
# root (/data/) — a bare "data/" also matched src/data/ (real source code:
# property.ts, pricing.ts, etc.) and silently kept it out of git entirely
# (found 2026-08-30 during server deploy — the build failed on the server
# because that whole directory was never committed/synced).
/data/

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# сирий дамп фото зі старого WordPress-сайту (для відбору, не для коміту як є)
legacy-assets/
